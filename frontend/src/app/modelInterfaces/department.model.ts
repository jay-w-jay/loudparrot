export interface Department {
    name: String;
    description: String;
    active: Boolean;
    maxEmployees: Number;
    minEmployees: Number;
    allowOvertime: Boolean;
    allowPenalty: Boolean;
}